# Copy to .env.local to point the app somewhere other than the docker-compose stack.
# Unset values fall back to the defaults in src/config.ts.
VITE_API_BASE_URL=http://localhost:8080
VITE_OIDC_AUTHORITY=http://localhost:8180/realms/getmyseat
VITE_OIDC_CLIENT_ID=getmyseat-frontend
